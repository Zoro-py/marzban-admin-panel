# ممیزی دور ۱ — F6: خط لولهٔ تسویهٔ ماهانهٔ payg (مهلت: شب ۲۲ سپتامبر ۲۰۲۶ / ۳۱ شهریور ۱۴۰۵)

**روش:** خواندن کامل `payg_monthly_job.py` + `settle_group`/`settle_account` (مسیر پول)، سپس
dry-run کامل روی کپی زندهٔ DB (۲۰۲۶-۰۹-۲۰ ۱۷:۰۳ UTC از کانتینر backend سرور فرانسه) با
Marzban و Telegram فیک. هارنس: `_vpn_scratch/f6_dryrun.py` (خارج از ریپو — DB کپی حاوی
توکن اشتراک واقعی است و هرگز کامیت نمی‌شود).

## محیط

- کپی DB: `sqlite .backup` داخل کانتینر → scp → `C:\Users\Laptop Markazi\Desktop\work\_vpn_scratch\vpn_live_20260920.db` (۱۲.۶MB)
- restore-test پاس شد: backend لوکال روی کپی بالا آمد؛ `/api/health`، `/api/reports/summary`،
  `/api/ledger/balance` هر سه scope (customer/group/account) با دادهٔ واقعی جواب دادند.
- سرور فرانسه `Etc/UTC` → تسویهٔ ۲۳:۳۰ UTCِ ۲۲ سپتامبر = ۰۲:۰۰ تهرانِ ۲۳ سپتامبر؛ تاریخِ
  محلیِ سرور در لحظهٔ اجرا هنوز ۲۲ سپتامبر است → هدف ۱۴۰۵-۰۶ ✓.

## یافته‌ها

| ID | شدت | یافته | شاهد | وضعیت |
|---|---|---|---|---|
| F6-1 | **P0** | فراخوانی مستقیم `settle_group`/`settle_account` از job ماهانه باعث می‌شد `operator` به آبجکت خام `Depends(require_auth)` بایند شود؛ اولین نوشتن `created_by` در commit خطا می‌داد (`Error binding parameter 6: type 'Depends' is not supported`)، **کل بچ rollback می‌شد — و مارکر ماه به‌هرحال جلو می‌رفت** (ماه «تسویه‌شده» علامت می‌خورد با صفر charge). dry-run قبل از رفع: `settled=0, failed=15` | dry-run روی کپی زنده؛ تست قرمز `tests/test_monthly_settle_direct_call.py` | **رفع شد** — کامیت `842b61b`: `operator="system:payg-monthly"` صریح + گارد RuntimeError در هر سه تابع settle |
| F6-2 | **P1** | سطر `MonthlySettlementBatch` گروه‌ها مبلغِ از‌پیش‌محاسبه را ذخیره می‌کرد نه مبلغ واقعاً شارژشده (settle دوباره از وضعیت زنده محاسبه می‌کند) → «ثبت تسویهٔ ماه قبل» مبلغ کهنه را credit می‌کرد: بدهی باقیمانده دائمی، یا اگر مصرف کم می‌شد credit جعلی. تست قرمز: batch=۵۰٬۰۰۰ ولی لجر=۶۰٬۰۰۰ | `test_monthly_settle_direct_call.py` سناریوی ۱۴۰۵-۰۷ با افزایش مصرف وسط اجرا | **رفع شد** — کامیت `30b9db8` |
| F6-3 | **P1** | `failed_resets` خروجی `settle_group` کاملاً دور ریخته می‌شد: عضوی که ریست Marzban‌اش شکست می‌خورد شارژ و baseline رول می‌شد (درست) ولی متر Marzban صفر نمی‌شد و **هیچ‌کس باخبر نمی‌شد** | dry-run partial: «failure visible to operator: False» | **رفع شد** — کامیت `30b9db8`: جمع‌آوری و پیام هشدار جدا از فهرست failed |
| F6-4 | P2 (ثبت‌شده، رفع نشده) | پیام «شکست» فقط اسم می‌آورد؛ اگر هر دو نوع شکست همزمان رخ دهند پیام‌ها جدا می‌آیند — قابل قبول؛ در بازبینی بعدی ارزیابی شود | — | پذیرفته‌شده |
| F6-5 | info | پیام‌های per-entity (آمادهٔ فوروارد) مبلغ قبضِ واقعاً شارژشده را نشان می‌دهند (بعد از F6-2)؛ سطر batch اکانت‌ها از قبل درست بود | کد | ✅ |

## نتایج dry-run بعد از رفع (کپی زنده، ساعت شبیه‌سازی‌شدهٔ ۲۲ سپتامبر ۲۳:۳۰)

- **مسیر سالم:** ۳ گروه + ۱۲ اکانت مستقل = ۱۵ مورد، جمع **۳٬۶۴۲٬۴۶۷.۴۸ تومان**؛ همه settle،
  batch×۱۵ ثبت، متر صفر (`usage_baseline == used_traffic`، `billable_bytes == 0`)، مارکر → `1405-06`؛
  trigger دوم همان شب و فردا = `already settled` (بدون دوبل‌شارژ).
  جزئیات: khosravi ۴۳۰٬۷۹۳ / faghih ۶۸۵٬۸۱۲ / kaffash ۱۳۳٬۸۰۷ / Azno ۹۱۷٬۱۷۶ / momen ۴۸۵٬۳۲۸ /
  Mmd ۱۵۹٬۱۵۶ / amirali_almasi ۳۲۵٬۹۴۴ / Sadegi ۱۳٬۴۰۵ و … (کامل در خروجی هارنس).
- **تلگرام قطع در گیت:** هیچ charge/reset/batch/marker — استثنا به scheduler می‌رسد و فردا retry می‌شود ✓
  (خود پیام هشدارِ شکستِ `failed` هم best-effort است؛ اگر تلگرام مرده باشد فردا با retry کل بچ درست می‌شود).
- **شکست جزئی:** ریست یکی از اعضای گروه تزریق شد → بقیه ۱۴+ مورد settle و ریست شدند؛ عضوِ خراب شارژ و
  baseline رول شد، ریست نشد، و (بعد از F6-3) به اپراتور اعلام می‌شود ✓
- **تقویم جلالی:** ۳۱/۳۰/۲۹ روز، سال کبیسهٔ ۱۴۰۳ (اسفند ۳۰)، روز نوروز، و روزِ قبل از آخر ماه
  (هدف = ماه قبلِ علامت‌خورده → no-op) — همه ✓ (`f6_dryrun.py cal`، ۱۱ چک)

## تست‌ها

- `backend/tests/test_monthly_settle_direct_call.py` (جدید، قرمز→سبز): خط لولهٔ واقعی با Marzban/تلگرام فیک —
  settle کامل، attributions، مبالغ، batch، مارکر، گارد فراخوانی مستقیم، و سناریوی افزایش‌مصرف-وسط-اجرا.
- کل سوت backend بعد از رفع: **۱۷/۱۷ سبز** (baseline کم نشده). `py_compile` تمیز.

## اقدام لازم برای مهلت ۲۲ سپتامبر

این دو کامیت (`842b61b`، `30b9db8`) باید قبل از ۲۲ سپتامبر ۲۳:۳۰ UTC روی production باشند
(فاز ۶). اگر همگرایی کامل تا آن زمان تمام نشد، دیپلوی هدفمند همین دو کامیت توصیه می‌شود
(بدون آن‌ها: اولین تسویهٔ با رفتار «متر صفر» به‌طور کامل و بی‌صدا شکست می‌خورد و ماه علامت می‌خورد).
